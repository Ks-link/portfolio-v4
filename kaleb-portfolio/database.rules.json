{
  "rules": {
    "play": {
      "global": {
        ".read": "auth != null",
        "host": {
          ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid || data.child('clientId').val().beginsWith(auth.uid + '_') || !newData.exists() && (data.child('uid').val() === auth.uid || data.child('clientId').val().beginsWith(auth.uid + '_')) || now - data.child('at').val() > 30000 && !root.child('play/global/presence').child(data.child('clientId').val()).exists() && !root.child('play/global/presence').child(data.child('uid').val()).exists())"
        },
        "slots": {
          ".write": "auth != null"
        },
        "presence": {
          "$id": {
            ".write": "auth != null && ($id === auth.uid || $id.beginsWith(auth.uid + '_'))"
          }
        },
        "inputs": {
          "$id": {
            ".write": "auth != null && ($id === auth.uid || $id.beginsWith(auth.uid + '_'))"
          }
        },
        "snapCells": {
          ".write": "auth != null && root.child('play/global/host/uid').val() === auth.uid"
        },
        "snapFood": {
          ".write": "auth != null && root.child('play/global/host/uid').val() === auth.uid"
        }
      }
    }
  }
}
